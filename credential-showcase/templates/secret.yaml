# Either remove this file completely since we're creating secrets in deploy-helm.sh
# Or if you want to keep it, update it to:
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Values.namespace }}
type: Opaque
data: {}  # Empty since we're managing secrets elsewhere